<Window x:Class="WpfClient.MVVM.View.AlertView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient.MVVM.View"
        mc:Ignorable="d"
        Title="AlertView" Height="450" Width="800">
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" d:Text="TItulo" Text="{Binding Tittle}" FontSize="16" Margin="10" HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="1" d:Text="Contenido del mensaje" Text="{Binding Message}" FontSize="16" Margin="10" HorizontalAlignment="Left"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Aceptar" Command="{Binding ConfirmCommand}" Width="75"/>
        </StackPanel>
    </Grid>
</Window>
