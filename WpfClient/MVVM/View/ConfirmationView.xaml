<Window x:Class="WpfClient.MVVM.View.ConfirmationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfClient.MVVM.View"
        mc:Ignorable="d"
        Title="ConfirmationView" Height="450" Width="800">
    <StackPanel Margin="20">
        <TextBlock Text="{Binding Message}" FontSize="16" Margin="10"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Aceptar" Command="{Binding ConfirmCommand}" Width="75" Margin="5"/>
            <Button Content="Cancelar" Command="{Binding CancelCommand}" Width="75" Margin="5"/>
        </StackPanel>
    </StackPanel>
</Window>
