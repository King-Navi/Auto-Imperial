<UserControl x:Class="WpfClient.MVVM.View.ClientCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfClient.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="600">
    <Border BorderBrush="Black" d:Background="Green" BorderThickness="1" Padding="10" Margin="5" CornerRadius="10" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock d:Text="Mauricio" Text="{Binding ClientActual.Name}" FontWeight="Bold" FontSize="14" Margin="10,0,50,0"/>
            <TextBlock d:Text="Sala" Text="{Binding ClientActual.PaternalSurname}" FontWeight="Bold" FontSize="14" Margin="10,0,10,0" />
            <TextBlock d:Text="Ramirez" Text="{Binding ClientActual.MaternalSurname}" FontWeight="Bold" FontSize="14" Margin="10,0,10,0" />
            <Button Content="Ver reservas" Command="{Binding NavigateToReserveViewCommand}" Padding="10,0,10,0" Margin="100,0,20,0" />
        </StackPanel>
    </Border>
</UserControl>
